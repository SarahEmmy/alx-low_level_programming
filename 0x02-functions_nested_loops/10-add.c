#include "main.h"

/**
  * add - adds two integers and returns the result
  * @a: parameter for add function
  * @b: parameter for add function
  * Return: 0 means success
  */
int add(int a, int b)
{
	return (a + b);
}
