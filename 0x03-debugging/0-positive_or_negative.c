#include <stdio.h>

/**
 * main - Start point for program
 * Return: Always 0 is success
 */
int main(void)
{
	int n = 0;
	if (n > 0)
	{
	printf("%d is positive\n", n);
	}
	else if (n >= 0)
	{
	printf("%d is zero\n", n);
	}
	else
	printf("%d is negative\n", n);
	return (0);
}
