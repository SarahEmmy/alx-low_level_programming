#include "main.h"

/**
  * _isdigit - checks for digit btw 0 - 9
  *
  * @c: character to check
  *
  * Return: 1 if digit and 0 otherwise
  */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
