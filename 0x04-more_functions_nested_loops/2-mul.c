#include "main.h"

/**
  * mul - multiplies two integers
  * @a: integer to multiply
  * @b: integer to multiply
  *
  * Return: 0 means success
  */
int mul(int a, int b)
{
	return (a * b);
	return (0);
}
